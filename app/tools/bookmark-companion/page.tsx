import BookmarkCompanion from '@/components/bookmark-companion';

export const metadata={
  title:'Chrome Bookmark Companion — StudyOS AI',
  description:'A free one-click Chrome bookmark bridge for saving learning context into StudyOS.'
};

export default function BookmarkCompanionPage(){
  return <BookmarkCompanion/>;
}
